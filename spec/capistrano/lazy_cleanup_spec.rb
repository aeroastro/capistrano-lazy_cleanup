RSpec.describe Capistrano::LazyCleanup do
  it "has a version number" do
    expect(Capistrano::LazyCleanup::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
